
/**
 * This is the top level package.
 * It contains data, exception, model and util sub-packages
 * "data" package has the data object classes that will encapsulate all the execution flow and execution step data for a given execution.
 * "exception" has the exception classes that will be used by the library
 * "model" package has the core classes that holds the execution rules for execution flow and various types of execution steps.
 * 			These do not hold any runtime data and the hierarchy of objects are initialized at the start up based on the configuration of execution flows and steps.
 * "util" package has any utility classes that are needed for the library
 * 
 */
package org.simple4j.wsorchestrator;